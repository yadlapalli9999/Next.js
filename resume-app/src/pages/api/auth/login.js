//import User from '../../../models/users';
//import {dbConncet} from '../../../util/index';

//dbConncet()
const handler = async (req,res) =>{
    if(req.method == 'POST'){
        console.log(req.body)
    }
     //res.status(200).json({name:'ramarao'})
}

export default handler;
